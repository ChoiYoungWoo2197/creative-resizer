# SAM 2 비-torch 의존성
# sam2 를 --no-deps 로 설치할 때 누락되는 패키지를 명시
# torch/torchvision 은 Dockerfile에서 CPU/GPU 분기 별도 설치
tqdm>=4.66.1
hydra-core>=1.3.2
omegaconf>=2.3.0
iopath>=0.1.10

# PSD 입력 지원 (Stage 18.1 — SAM2 이전에 설치해 Dockerfile 단계 검증 가능)
psd-tools==1.9.31
